# 复制本文件为 .env 并填入你的 API key（.env 已被 .gitignore 排除，不会上传）
#
# DeepSeek（推荐，便宜）：https://platform.deepseek.com 注册后创建 API key
DEEPSEEK_API_KEY=<在此粘贴你的DeepSeek_API_Key>

# OpenAI（可选）
# OPENAI_API_KEY=<在此粘贴你的OpenAI_API_Key>

# ---- 本地 Ollama（可选，无需 API key，用本地 GPU 跑 Agent）----
# 安装: https://ollama.com → 启动后: ollama pull qwen2.5:7b
# OLLAMA_MODEL=qwen2.5:7b
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_PREFER=1            # 设为 1 时 auto 后端优先用本地模型
