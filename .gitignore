# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Gradio
.gradio/
.gradio/certificate.pem

# 环境变量与虚拟环境
.env
.venv/
venv/

# 临时文件
*.tmp
*.egg-info/

# ML 产物（模型 / 图片 / 缓存）
ml/artifacts/
artifacts/ml_models/
*.joblib
*.npz
*.png
ml/__pycache__/
ml/**/__pycache__/

# Hugging Face / ESM 本地缓存（如通过环境变量配置到仓库内）
.huggingface/
huggingface_cache/
.cache/huggingface/
model_cache/
tmp_embeddings/

# 端到端演示产物（可再生成）
demo/

# VSCode（本机路径配置，不提交）
.vscode/

# 本地私有笔记（岗位要求与映射，仅本地使用）
LOCAL_JOB_NOTES.md

# 文献检索缓存（SQLite，可再生成，绝不提交）
literature_cache/
*.db

# 其他工具的工作日志/记忆（如 CodeBuddy .workbuddy），不进库
.workbuddy/

# v9 真实验收证据(不提交)
_v*.json
*_retest.json

# 本地辅助脚本
run_utf8.bat

# Desktop 批量导出的本地产物（示例输入 example_antibodies.csv 仍保留）
antibody_batch_results.csv
antibody_batch_results.xlsx
antibody_batch_template.xlsx
